export async function data(path){const r=await fetch(path);if(!r.ok)throw new Error(path);return r.json()}
export function money(c){return new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(c/100)}
export function reveal(){const items=document.querySelectorAll(".reveal");if(!("IntersectionObserver" in window)){items.forEach(x=>x.classList.add("visible"));return}const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");o.unobserve(e.target)}}),{threshold:.1});items.forEach(x=>o.observe(x))}
export async function shell(){const s=await data("data/site.json");const page=document.body.dataset.page;document.querySelector("#site-header").innerHTML=`<header class="site-header"><div class="container nav-row"><a class="brand" href="index.html"><img src="assets/logo.png" alt=""><span>${s.name}</span></a><button class="menu-toggle" type="button" aria-expanded="false" aria-controls="nav">Menu</button><nav class="nav" id="nav" aria-label="Navegacao principal"><a href="index.html" class="${page==='inicio'?'active':''}">Inicio</a><a href="agenda.html" class="${page==='agenda'?'active':''}">Agenda</a><a href="loja.html" class="${page==='loja'?'active':''}">Loja</a><a href="galeria.html" class="${page==='galeria'?'active':''}">Galeria</a></nav></div></header>`;document.querySelector("#site-footer").innerHTML=`<footer class="footer"><div class="container footer-row"><a class="brand" href="index.html"><span>${s.name}</span></a><p>© ${new Date().getFullYear()} ${s.name}. A rua e nossa.</p><a href="#conteudo">Voltar ao topo</a></div></footer>`;document.querySelectorAll("[data-instagram]").forEach(a=>a.href=s.instagram);document.querySelectorAll("[data-email]").forEach(a=>a.href=`mailto:${s.email}`);const b=document.querySelector(".menu-toggle"),n=document.querySelector(".nav");b?.addEventListener("click",()=>{const open=n.classList.toggle("open");b.setAttribute("aria-expanded",open)});document.addEventListener("keydown",e=>{if(e.key==='Escape'){n?.classList.remove('open');b?.setAttribute('aria-expanded','false')}});return s}