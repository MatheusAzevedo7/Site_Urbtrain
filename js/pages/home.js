import{data,reveal,shell,money}from"../components/core.js";await shell();const[site,products]=await Promise.all([data("data/site.json"),data("data/produtos.json")]);document.querySelector("#stats-grid").innerHTML=site.stats.map(x=>`<div class="reveal"><strong>${x.value}</strong><span>${x.label}</span></div>`).join("");document.querySelector("#product-preview").innerHTML=products.map(p=>`<article class="product-card reveal"><img src="${p.image}" alt="${p.name}" loading="lazy"><div><p class="eyebrow dark">${p.category}</p><h3>${p.name}</h3><strong>${money(p.price)}</strong></div></article>`).join("");reveal();