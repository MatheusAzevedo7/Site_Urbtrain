import{data,reveal,shell}from"../components/core.js";await shell();const items=await data("data/galeria.json"),grid=document.querySelector("#gallery-grid"),m=document.querySelector("#gallery-modal"),mi=document.querySelector("#gallery-modal-image"),cap=document.querySelector("#gallery-modal-caption");grid.innerHTML=items.map((x,i)=>`<button class="gallery-item reveal" type="button" data-i="${i}"><img src="${x.image}" alt="${x.alt}" loading="lazy"></button>`).join("");let last;const close=()=>{m.classList.remove("open");m.setAttribute("aria-hidden","true");last?.focus()};grid.onclick=e=>{const b=e.target.closest("button");if(!b)return;last=b;const x=items[b.dataset.i];mi.src=x.image;mi.alt=x.alt;cap.textContent=x.caption;m.classList.add("open");m.setAttribute("aria-hidden","false");m.querySelector("button").focus()};m.querySelector("button").onclick=close;m.onclick=e=>{if(e.target===m)close()};document.addEventListener("keydown",e=>{if(e.key==='Escape'&&m.classList.contains('open'))close()});reveal();